1、指令根据 /Users/hjt/Projects/自动化测试/模版文档/仿真通用模版总结.md 和 /Users/hjt/Projects/自动化测试/模版文档/示例_指令模版.json这两个文件，除了之前的要求外，这些字段都用户可配置，并且也可以对同一种实体，进行批量添加同一种行动，生成不同的aciton_id。
添加行动也加一张表到数据库，每条记录，包含 rule_type_code ，xdmc，加上这个示例其它所有 action 和 ruleconfig 这一级的所有字段，除了 rule_type_code和xdmc有对应的字符串值意外事件，其它的都是bool，true的是该行动必填，显示在前端，让用户填写该字段下的所有项目，为false 该种行动不用管该字段，生成json可不生成。
