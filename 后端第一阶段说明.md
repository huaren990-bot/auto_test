# 想定编辑后端第一阶段

## 范围

本阶段将装备型号目录和行动类型目录迁移到本地 SQLite 数据库。想定编辑器启动后通过 HTTP 接口读取型号目录，用户在“模型与行动”中新增或导入型号时直接写入数据库。方案、实体、行动和模拟运行记录暂时继续保存在浏览器，后续阶段再迁移。

一条数据库记录代表一个具体型号。`mxlx` 是唯一主键；`mxmc` 是前端显示名称，不要求唯一，因此同一名称可以对应多个不同型号。前端构造实体时用 `mxlx` 绑定所选记录，生成想定时带入该记录的全部模型字段。

## 本地数据库

默认文件：`data/simtest.sqlite`

装备业务表：`equipment_models`；新增行动业务表：`action_types`。行动表的 21 个布尔字段、接口及构建规则见 [行动指令构建说明](行动指令构建说明.md)。

| 字段 | 约束 | 用途 |
| --- | --- | --- |
| mxlx | 主键 | 具体装备型号及前端内部选择标识 |
| FirstCfn | 必填 | 一级分类 |
| SecondCfn | 必填 | 二级分类 |
| ThirdCfn | 必填 | 三级分类 |
| ForthCfn | 必填 | 四级分类 |
| mxmc | 必填、可重复 | 前端显示的装备名称 |
| mxnm | 唯一 | 模型编码；未填写时使用 mxlx |
| lxzymc | 系统可补齐 | 资源类型名称；未填写时使用 mxmc |
| lxzynm | 唯一、系统可生成 | 资源类型编号；未填写时生成 UUID |
| bzlx | 可空 | 编队类型默认值 |
| commandType | 必填 | 指令实体类型映射 |
| description | 可空串 | 型号说明 |
| created_at、updated_at | 系统维护 | 数据记录时间 |

首次启动会自动建表，并写入当前三个样例型号。浏览器中已有的自定义型号会在首次连接时尝试批量迁移；数据库记录是后续读取的权威来源。

## HTTP 接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/health` | 检查服务及数据库连接，返回型号数量 |
| GET | `/api/models` | 返回全部型号；可按四级分类字段查询 |
| POST | `/api/models` | 新增一个具体型号 |
| POST | `/api/models/import` | 原子批量导入型号，任意一条错误时整批不写入 |
| GET / POST | `/api/action-types` | 读取或新增行动类型 |
| PUT | `/api/action-types/:code` | 更新行动名称和字段开关 |
| POST | `/api/generate` | 校验当前方案并返回配套想定与指令 JSON |

新增单条型号的请求示例：

```json
{
  "id": "AIRCRAFT-001",
  "FirstCfn": "物资",
  "SecondCfn": "空中",
  "ThirdCfn": "飞机",
  "ForthCfn": "固定翼",
  "mxmc": "某型飞机",
  "mxlx": "AIRCRAFT-001",
  "mxnm": "CMRM0300000000101",
  "lxzymc": "某型飞机",
  "lxzynm": "f4d4d78e-515c-47ca-bba2-5069ad254982",
  "bzlx": "AIRCRAFT-001",
  "commandType": "AIRCRAFT-001",
  "description": "固定翼飞机型号"
}
```

也可以只提交四级分类、`mxmc` 和 `mxlx` 六个业务字段。分类值无需预先配置，随型号记录写入后会自动出现在前端分类选择中。例如：

```json
{
  "FirstCfn": "物资",
  "SecondCfn": "海上",
  "ThirdCfn": "舰船",
  "ForthCfn": "驱逐舰",
  "mxmc": "00驱逐舰",
  "mxlx": "00qzj"
}
```

接口响应会补充 `id`，其值始终等于 `mxlx`，供现有前端组件作为稳定选项值使用。界面显示仍使用 `mxmc`。

## 启动与检查

```bash
npm start
npm run db:models
```

可用以下请求直接检查：

```bash
curl http://127.0.0.1:4173/api/health
curl http://127.0.0.1:4173/api/models
```

可通过 `SIMTEST_DB_PATH=/绝对路径/models.sqlite npm start` 指定其他数据库文件。
