# SimTest 仿真自动化测试工具 软件设计文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0（对应软件 v0.3.0） |
| 配套文档 | [产品设计文档_v1.0.md](产品设计文档_v1.0.md)、[后端第一阶段说明.md](后端第一阶段说明.md)、[行动指令构建说明.md](行动指令构建说明.md)、[需求描述.md](需求描述.md) |
| 代码规模 | 源码约 1700 行（backend 204 行 / frontend 827 行 / scripts 97 行 / tests 415 行） |
| 技术栈 | Node.js ≥22（无第三方 npm 依赖）、原生 ES Module、SQLite（`node:sqlite`）、原生 JavaScript 单页应用 |

本文档描述当前已实现软件的系统设计：架构、模块、数据模型、接口、核心流程与质量属性。产品设计层面的目标、页面设计与验收标准见产品设计文档；本文聚焦"软件如何实现"。

---

## 1 概述

### 1.1 系统定位

SimTest 是仿真自动化测试工作台。核心使命：**用户维护一份"方案"（想定实体 + 行动指令），系统据此自动构建两份结构一致的仿真输入 JSON（想定、指令），调用外部仿真器执行，收集产物并逐行动判读**。

系统解决三类手工成本：

1. **引用一致性**——两份 JSON 中实体 ID、目标引用、武器编码、时间范围由生成器从同一份方案数据重建，用户不再手抄字段。
2. **一致性校验**——生成前执行三层校验（行动类型、行动实例、整体方案），错误阻断文件发布。
3. **结果可判读**——运行产物与行动按事件关联，结论区分"通过 / 不通过 / 待复核 / 未判读"，不确定时保留原始证据而非臆断成功。

### 1.2 当前实现状态

| 能力域 | 状态 |
| --- | --- |
| 装备型号（SQLite 持久化、四级分类、分页检索、批量导入） | 已实现，已接入 |
| 行动类型（字段开关表、动态嵌套表单、批量添加） | 已实现，已接入 |
| 方案（SQLite 持久化、浏览器回退、首次迁移导入） | 已实现，已接入 |
| 想定 / 指令 JSON 生成（后端校验 + 生成） | 已实现，已接入 |
| 地图布点（离线 Natural Earth 底图、聚类、框选、拖动） | 已实现 |
| 运行中心（串行模拟运行、进度、判读展示、复盘轨迹） | 前端模拟实现，未调用外部程序 |
| 外部仿真器 / 复盘程序调用 | 接口约定已定义（产品设计 §6），未实现 |
| 真实产物判读（事件关联规则引擎） | 初始规则框架已定义，判读结果当前为模拟 |
| 设置持久化、运行记录持久化 | 仍存浏览器 localStorage |
| Linux 离线部署（deb / systemd / 便携包） | 已实现 |

---

## 2 总体架构

### 2.1 分层结构

```text
┌─────────────────────────────────────────────────────────────┐
│ 浏览器 SPA（frontend/，无框架、无构建步骤）                     │
│   app.js 页面/路由/事件        api.js 数据访问适配器             │
│   core.js 领域纯函数            action-schema.js 行动契约        │
│   action-ui.js 动态表单         map.js SVG 地图                 │
└──────────────┬──────────────────────────────────────────────┘
               │ fetch JSON（同源，127.0.0.1:4173）
┌──────────────▼──────────────────────────────────────────────┐
│ Node 后端（scripts/serve.mjs，node:http 单进程）              │
│   路由分发 → 错误映射（mapError） → Store 层                  │
│   ┌────────────┬─────────────┬─────────────┐                │
│   │ ModelStore │ ActionStore │  PlanStore  │                │
│   └─────┬──────┴──────┬──────┴──────┬──────┘                │
│         └──────────────┼─────────────┘                      │
│                data/simtest.sqlite（WAL 模式，单文件）         │
│   静态文件服务：frontend/ 只读目录                             │
└──────────────┬──────────────────────────────────────────────┘
               │（目标态：进程/服务适配器）
┌──────────────▼──────────────────────────────────────────────┐
│ 外部仿真器 · 复盘推演程序（可执行程序 + 参数模板，占位符展开）     │
└──────────────────────────────────────────────────────────────┘
```

设计要点：

- **单进程、零依赖**。`node:sqlite`（Node 22 内置）+ `node:http`，无 npm 依赖，离线部署只需 Node 运行时（打包时内嵌 node v22.23.2）。
- **领域逻辑前端化、后端薄化**。校验与 JSON 生成（`core.js` / `action-schema.js`）是纯函数，前端直接调用以即时反馈；后端 `/api/generate` 复用同一份 `generate()` 实现，避免前后端逻辑漂移。后端只负责持久化与"代码已登记"这类需要数据库的事前检查。
- **混合持久化与回退**。型号、行动类型、方案走 SQLite；设置与运行记录仍走 localStorage。后端不可达时前端自动降级为纯浏览器模式并显示"方案本地回退"徽标，不阻断编辑。
- **前端无构建步骤**。`frontend/index.html` 直接以 ES Module 加载 `app.js`，服务端按扩展名返回 Content-Type，`Cache-Control: no-store`。

### 2.2 目录结构

```text
frontend/            浏览器应用（无构建，直接服务）
  index.html           页面壳（#app / #modal-root / #toasts）
  app.js               路由、页面渲染、事件代理、脏检查、快捷键
  core.js              领域纯函数：实体/行动构造、引用规则、校验、JSON 生成、ZIP
  api.js               数据访问适配器：HTTP 请求、localStorage、回退、模拟运行
  action-schema.js     行动字段结构定义、类型校验、实例校验、输出投影（前后端共享契约）
  action-ui.js         行动类型目录、字段开关编辑、动态嵌套表单、批量添加
  map.js               SVG 地图：投影、底图、聚类、框选、拖动
  styles.css           布局与组件样式
  assets/land.geojson  Natural Earth 离线底图（含第三方来源说明 THIRD_PARTY.md）
backend/             SQLite 存储层（仅被 serve.mjs 使用）
  model-store.mjs      equipment_models 表：校验、增删查、分页、原子批量导入
  action-store.mjs     action_types 表：字段开关、建表迁移、种子类型
  plan-store.mjs       plans 表：整对象 JSON 载荷、upsert、缺失导入
scripts/
  serve.mjs            HTTP 服务器：API 路由 + 静态文件（createSimTestServer 可测试导出）
  list-models.mjs      数据库型号查看命令
  build-offline-linux.sh  离线 deb / tar.gz 构建（内嵌 Node、校验 SHA256）
data/                simtest.sqlite（WAL，-shm/-wal 伴生文件）、backups/
tests/               node --test 单元测试（35 例，全通过）
output/playwright/   浏览器端到端脚本与截图（独立测试数据库与浏览器存储）
packaging/linux/     deb 控制文件、systemd unit、simtestctl、离线安装文档
dist/                构建产物（simtest 0.3.0 deb + 离线 tar.gz + SHA256）
```

### 2.3 质量属性与实现手段

| 属性 | 手段 |
| --- | --- |
| 一致性 | 生成器从单一方案数据重建全部引用；方案保存 revision；运行冻结输入快照 |
| 可用性 | 后端不可达时浏览器回退；恢复后按 ID 补迁移，不重复导入内置示例 |
| 原子性 | 型号批量导入、方案导入使用 `BEGIN IMMEDIATE` 事务，整批成功或整批回滚 |
| 安全性 | 静态服务路径逃逸检查（`file.startsWith(root+sep)`）、25 MB 请求体上限、前端 `esc()` 转义、SQL 全部参数绑定 |
| 可恢复性 | 页面重载时在途模拟运行标记 `interrupted` 而非假成功 |
| 可移植性 | 零 npm 依赖；离线安装包内嵌 Node 并做 SHA256 校验 |
| 可追溯性 | 每次运行目录唯一，输入/产物/日志/判读全部留存；运行不写外部草稿 |

---

## 3 前端模块设计

### 3.1 模块职责与依赖

```text
        index.html
            │  <script type="module" src="./app.js">
            ▼
       app.js ──────────────────────────────────────┐
       路由/页面/事件/脏检查                          │ 注入依赖（api, validate…）
            │           │                           ▼
            │   core.js  ◄──────────────  action-ui.js
            │   (纯函数: 实体/行动/     action-schema.js ◄──┐
            │    校验/generate/zip)                        │ 共享契约（后端亦 import）
            │   api.js                                  │
            │   (localStorage + fetch + 模拟运行)  backend/*（经 HTTP，非直接 import）
            │   map.js (mountMap，自渲染)                │
            ▼                                            │
       #content DOM（innerHTML 重渲染 + simtest:update 事件）
```

**依赖注入而非全局单例**：`actionUI({api, getPlan, ...})` 以参数接收协作者，模块无隐式全局状态（`map.js` 除外，其视图状态缓存按 `stateKey` 存 Map）。

### 3.2 core.js —— 领域内核（169 行，纯函数）

唯一允许被后端复用的前端模块（`serve.mjs` 的 `/api/generate` 直接 `import { generate } from '../frontend/core.js'`）。

| 导出 | 职责 |
| --- | --- |
| `uid / numericId` | 无连字符 UUID；33 位数字部队 ID（`BigInt('0x'+uid)` 截断补齐，沿用样例 33 位格式） |
| `classificationFields` | 四级分类键 `FirstCfn..ForthCfn` 及中文标签 |
| `mountKinds` | 三种挂载关系：`ammo`（武器）、`Hang`（机库/飞机）、`AmDp`（武器库/武器，要求先有 Hang） |
| `modelMountRole / mountKindAcceptsModel` | 按分类正则判断模型角色（aircraft/weapon），约束"关系×模型"合法性 |
| `models` | 内置种子型号（MX01 设施、MX02 飞机、AMMO01 弹药），后端不可达时的兜底目录 |
| `entityId(e)` | 对外引用 ID 规则：有 `bzlx` 取 `llbznm`，否则取 `zyId` |
| `deployedEntities / mountedEntities / mountedWeaponEntities` | 实体树查询：地图实体 / 挂载子实体 / 武器类挂载 |
| `createEntity / createEntityBatch` | 单实体与方阵批量（≤50，按 √n 列排布、0.08° 间距、坐标钳制） |
| `createMountedEntityBatch` | 挂载实体批量；校验关系×模型、强制与父同阵营、继承父坐标 |
| `createPlan / createAction / copyPlan` | 方案/行动构造；`copyPlan` 重建实体 ID 映射并重写所有深层引用（递归 rewrite） |
| `validate(p)` | 方案级校验，返回 `{errors, warnings}`（见 §7） |
| `generate(p, title)` | 想定 + 指令 JSON 生成（见 §8） |
| `pairNames(f)` | 生成对文件名：想定 `{ybnm}.json`，指令 `{ybnm}_xdzl.json`，两者永不重名 |
| `makeZip(files)` | 手写 STORE 方式 ZIP 写入器（含 CRC32），无依赖导出配套两份文件 |

### 3.3 api.js —— 数据访问适配器（154 行）

页面唯一数据入口。对上层（`app.js`）暴露异步方法，内部按就绪状态在三处之间路由：

```text
页面调用 api.savePlan(p)
        │
        ├─ 型号目录:  catalogMode === 'backend' ? POST/GET /api/models : 内置 models 兜底
        ├─ 行动目录:  actionCatalogReady ? /api/action-types : 内存 actionTypes 过滤分页
        ├─ 方案:      planReady ? PUT /api/plans/:id : localStorage
        └─ 运行/设置: 恒为 localStorage（模拟）
```

关键机制：

- **首次迁移**：`initialize()` 将浏览器旧型号中远端缺失者经 `/api/models/import` 补入；`initializePlans()` 同理补方案（`isBuiltinDemoPlan` 判定内置演示方案，不重复导入）。远端有数据时以远端为准，否则保留本地。
- **版本迁移（localStorage v→v6）**：启动时执行一次性迁移链——废弃型号（HANG01/AMDP01）映射到现役型号并重挂；旧 `ammo/hang/equipment` 计数字段展开为挂载实体；行动类型快照剔除已废弃的 `start_time/end_time` 时间开关（时间改为全局必填）；迁移过的方案 `generated=null, revision+1` 使旧文件作废。
- **回退语义**：每个初始化方法独立 try/catch，置 `planMode/planReady/catalogMode` 标志；顶栏徽标与黄色警告条展示回退原因。写操作失败时降级到 localStorage 并提示"已保留在浏览器"。
- **`simtest:update` 事件**：每次持久化（`persist` + `emit`）后派发 CustomEvent，`app.js` 监听后在非编辑页重渲染，编辑页（draft）不重绘以保护输入焦点。
- **模拟运行器**：`setInterval(650ms)` 驱动的串行队列——取队首 queued → running，进度 +9/ tick，80% 转 collecting，100% 调 `complete()`；`outcome==='error'` 在 45% 处转 failed（日志"仿真器退出码 1，已隔离，队列继续"）。全部产物标注 `mock:true`。

### 3.4 action-schema.js —— 行动契约（141 行，前后端共享）

行动系统的单一事实来源，定义三个层级的结构：

| 导出 | 内容 |
| --- | --- |
| `actionFields` | 14 个**参数分支**：`action_id, related_force, executor, target, launch, region, route, radar_angle, upstream_entity, downstream_entity, supply, start_time, end_time, jam`。每个是 schema 对象（type/label/min/max/ref/optional），既是校验器又是**表单生成器**（递归 `schemaDefault`） |
| `configFields` | 9 个**策略字段**：`is_auto_attack, is_auto_return, is_task_continue, is_hidden_maneuver, is_consider_environment, is_auto_deterrence, is_deterrence_auto_return, radar_detect_mode, intercept_strategy` |
| `flagFields` | = actionFields + configFields 去掉时间两项 = **类型表的布尔开关列集合** |
| `defaultActionType` | 种子类型 `000680 示例行动`：`action_id/related_force/executor/target` + 全部策略字段为 true |
| `requiredActionFields` | `start_time/end_time` 固定必填，不受类型开关控制 |

核心函数：

- `validateActionType(type)`：代码必须 6 位数字串、名称必填、全部 flag 必须布尔、拒绝未知字段。
- `normalizeActionType(type)`：剔除历史遗留的时间 flag（兼容旧快照）。
- `validateValue(schema, value, path)`：递归结构校验（对象/数组/字符串/布尔/数值范围，`optional` 可省）。
- `projectValue(schema, value)`：输出投影——`optional` 字段为 undefined 或空数组时**整枝省略**，保证"未启用分支不输出"。
- `configuredOutput(a)`：实例 → `{action, config}`。`action` 含 `rule_type_code` + 启用参数（`action_id` 取实例 ID）；`config` 含启用的策略字段，按执行者+相同策略分组进 `ruleconfig`。
- `validateConfiguredAction(a, plan)`：实例级交叉校验（见 §7.2）。
- `makeConfiguredAction(definition, values, owner)` / `syncActionTargets(a, plan)`：从类型快照+默认值建实例，并把 `target_id` 反向同步为页面目标选择。

### 3.5 action-ui.js（100 行）

- 行动类型目录：本地过滤 + 远端分页缓存（`catalogState` 按 `query+page` 键缓存，`sequence` 防竞态），显示启用字段预览（前 4 项 + "…"）。
- 类型编辑对话框：名称 + 代码（编辑时禁用）+ `flagFields` 布尔勾选网格；`start_time/end_time` 不出现（固定必填）。
- 动态表单渲染器：按类型快照对 `allFields` 递归生成嵌套输入（string/number/boolean 复选/object 分组/array 条目增删，`ref` 属性渲染为对应当前方案的实体/武器下拉）。

### 3.6 app.js（352 行）

- **hash 路由**：`#/plans`、`#/plans/:id`（tab：info 基本信息 / map 想定编辑（内切地图·表格视图）/ actions 行动指令 / files 生成与校验）、`#/runs`、`#/runs/:id`、`#/library`、`#/settings`；`go()` 带脏检查（未保存编辑时弹离开确认）。
- **渲染策略**：整页 `render()` 重建 innerHTML + 事件委托（`[data-act]` 统一分发）；地图等重部件由 `mountMap` 按 `stateKey` 重挂载，视图状态（center/span）跨重渲染保留。
- **draft 模型**：编辑工作区持有 `ui.draft` 深拷贝，`ui.dirty` 标记；Ctrl/Cmd+S 保存；`api` 快照更新不覆盖草稿。

### 3.7 map.js（56 行）

无第三方地图库的 SVG 实现：

- 等距圆柱投影 `project/invert`，WGS84 经纬 → 1000×700 viewBox；Natural Earth 陆地 GeoJSON 一次 fetch 缓存。
- 交互：指针拖平移、点击/拖拽选点（placement 模式放实体）、矩形框选（`onBoxSelect` 多选）、标记拖动改坐标（四舍五入 4 位）、滚轮/按钮缩放（span 1–120°）、⌖ 定位全部实体。
- 性能：42×42 px 网格 bin 聚类，同 bin 多实体合并为数字簇（点击簇放大 0.55 倍 span 展开）；`requestAnimationFrame` 合帧重绘。

---

## 4 后端模块设计

后端是"薄"层：路由 → 读体 → Store → 错误映射。

### 4.1 serve.mjs（85 行）

- `createSimTestServer({root, databasePath})` 导出工厂（测试可注入临时库）；直接运行时监听 `PORT`（默认 4173）/ `HOST`（默认 127.0.0.1），数据库路径 `SIMTEST_DB_PATH` 可覆盖。
- 路由表见 §9。请求体流式读取，超 25 MB 抛 413，空体 400，非法 JSON 400。
- `mapError`：`ModelValidationError/PlanValidationError` → 400（带 `statusCode` 的取自带值）；SQLite 约束冲突（`errcode & 0xff === 19`）→ 409；`ActionTypeError` 自带 statusCode；其余 → 500 泛化消息。
- `/api/generate`：请求体即完整方案 → 逐行动检查 `actions.get(action.code)`（代码必须已登记）→ 调用共享 `generate()`，异常包成 `ActionTypeError`（400，不返回半成品）。
- 静态服务：`GET/HEAD` 非 `/api/` 路径 → `frontend/` 下文件；`resolve` 后前缀校验防目录逃逸（403）；MIME 表覆盖 html/js/css/json/geojson/svg；`Cache-Control: no-cache`。

### 4.2 ModelStore（135 行）

`equipment_models` 表的完整实现，建表 + `PRAGMA user_version = 1` 占位后续迁移：

```sql
equipment_models(
  mxlx TEXT PRIMARY KEY,          -- 型号编号 = 前端稳定选项值 id
  FirstCfn..ForthCfn TEXT NOT NULL,  -- 四级分类（组合索引）
  mxmc TEXT NOT NULL,             -- 显示名称
  mxnm TEXT NOT NULL UNIQUE,      -- 模型编码
  lxzymc TEXT NOT NULL,           -- 类型名称
  lxzynm TEXT NOT NULL UNIQUE,    -- 资源类型 UUID
  bzlx TEXT,                      -- 部队类型（可空）
  commandType TEXT NOT NULL,      -- 指令 entity_type 映射
  description TEXT NOT NULL DEFAULT '',
  created_at/updated_at TEXT
)
INDEX idx_equipment_models_classification(FirstCfn, SecondCfn, ThirdCfn, ForthCfn)
```

- `normalizeEquipmentModel`：六个业务字段（分类×4 + mxmc + mxlx）必填；可选字段缺省推导（`mxnm←mxlx`、`lxzymc←mxmc`、`lxzynm←randomUUID`、`commandType←mxlx`、`bzlx←null`）；`id` 若提供必须等于 `mxlx`。
- `list(filters)`：按提供的分类等值过滤，固定四级分类 + 名称排序。
- `search({filters, query, page, pageSize})`：分类等值 + `q` 对 mxmc/mxlx/mxnm/分类 LIKE；`pageSize` 钳制 1–100（默认 50），返回 `{items,total,page,pages,pageSize}`。
- `create / createMany`：批量先全量归一化 + 批内查重（mxlx/mxnm/lxzynm），`BEGIN IMMEDIATE` 单事务，失败整体 ROLLBACK。
- `seed()`：内置型号 `INSERT OR IGNORE`，幂等。

### 4.3 ActionStore（24 行，单行压缩风格）

```sql
action_types(
  rule_type_code TEXT PRIMARY KEY,
  xdmc TEXT NOT NULL,
  <flagFields 各列> INTEGER NOT NULL CHECK (col IN (0,1))  -- 列集合由前端 schema 派生
)
```

- 建表时**从 `flagFields` 动态生成布尔列**——schema 增删字段即表结构演进（配合启动期 `PRAGMA table_info` 迁移：废弃列 `DROP COLUMN`，事务保护）。
- 种子保证至少存在 `000680`。`decode` 把 0/1 还原为布尔。
- `check` 复用前端 `validateActionType`；`create` 冲突 409；`update` 禁止改代码（路径与体不一致即拒）。

### 4.4 PlanStore（45 行）

方案整体作为 JSON 载荷存单列，避免过度规范化（方案内实体/行动的引用完整性由领域层校验）：

```sql
plans( id TEXT PRIMARY KEY, name TEXT NOT NULL,
       archived INTEGER NOT NULL DEFAULT 0 CHECK (archived IN (0,1)),
       revision INTEGER NOT NULL DEFAULT 0, updated_at TEXT NOT NULL,
       payload TEXT NOT NULL )
INDEX (archived, updated_at DESC)
```

- `normalizePlan`：id 非空、name ≤60 字符、entities/actions 必须数组、revision 安全整数、`JSON.stringify` 往返确认可序列化。
- `upsert`：`ON CONFLICT(id) DO UPDATE SET ...` 全字段覆盖（乐观锁列为后续演进项）。
- `importMissing`：批内查重 → 事务内逐条 `if (get(id)) continue`，只补缺失。

---

## 5 数据模型

### 5.1 核心对象关系

```text
方案 Plan (1)───(N) 实体 Entity
                   ├─ 地图实体（parentId=null）：阵营 Blue/Red、模型快照、llbznm/sjllbznm/zyId、经纬高
                   └─ 挂载实体（parentId=地图实体.id）：挂 ammo/Hang/AmDp 之一，与父同阵营
方案 Plan (1)───(N) 行动 Action
                   ├─ executor → 地图实体（挂载实体不可执行）
                   ├─ targets[] → 对方阵营地图实体
                   ├─ code → action_types.rule_type_code（登记前置条件）
                   ├─ definition = 类型快照（冻结）
                   └─ values = 按 snapshot 填写的嵌套值
方案 Plan (1)───(N) 生成版本 generated {ybnm, scenario, commands, revision, created}
方案 Plan (N)───(1) 批次 Batch (1)───(N) 运行 Run
                                   └─ {status, progress, logs[], results[], verdict, review}
型号 Model（equipment_models 目录）◄── 实体.model 快照
行动类型（action_types 目录）◄── 行动.definition 快照
```

**快照语义**（关键约定）：实体持有 `model` 深拷贝、行动持有 `definition` 深拷贝。目录更新只影响**之后新增**的实体/行动；已保存实例不随目录漂移。方案级"更新配置"是显式操作（设计文档 §8）。

### 5.2 持久化分布

| 数据 | 位置 | 原因 |
| --- | --- | --- |
| equipment_models | SQLite | 全局共享目录，需跨浏览器一致 |
| action_types | SQLite | 同上 |
| plans（完整对象 JSON） | SQLite + localStorage 双写 | 主体数据入库存，本地副本作回退与未连接时编辑 |
| settings（路径/仿真器/超时） | localStorage | 单机个人偏好，待后端设置服务 |
| runs（模拟运行记录） | localStorage | 前端模拟产物，待运行持久化服务 |
| 底图 land.geojson | 静态文件 | 只读资产 |

localStorage 键 `simtest-demo-v1`，`db.version=6` 携带迁移链；写入失败（配额）置 `storageError` 并顶栏警告。

---

## 6 文件生成设计（想定 / 指令）

### 6.1 生成入口

`generate(plan, title?)`（core.js）：

1. `validate(plan)` 有 error 即抛错——**校验失败不产出任何文件**。
2. `ybnm`：标题参数，或 `方案名(非法字符→_,截60) + 时间戳 + 6 位随机`，保证不同生成版本可区分（需求：批量调用 ybnm 不得相同）。
3. 输出 `{ybnm, scenario, commands, revision, created}`，由后端 `/api/generate` 返回 `{files: ...}`；前端 `makeZip` 按 `pairNames` 打包为 `scenario/{ybnm}.json` + `command/{ybnm}_xdzl.json` 供下载（指令文件带 `_xdzl` 后缀，不与想定重名）。

### 6.2 想定 JSON 结构

```jsonc
{
  "cjlx": "DEMO_TYPE",                 // 固定样例类型，配置可替换
  "startTime": "2027-01-01 10:00:00",  // 方案时间
  "endTime":   "2027-01-01 13:30:00",
  "ybnm": "基础行动验证_1694…_a1b2c3",
  "yb": [                               // 恒生成 01/02 两方
    { "dwsxnm": "01",
      "data": { "resource": { "dwsxnm": "01", "trooplist": [
        { "bzmc": "蓝方平台 01", "bznm": "", "glph": "", "jznm": null,
          "bzlx": null, "llbznm": "0123…(33位)", "sjllbznm": "…",
          "jd": 120.1, "wd": 30.2, "gc": 0,
          "lstEquip": [ { "lxzymc": "…", "lxzynm": "uuid", "mxlx": "…",
                          "mxmc": "…", "mxnm": "…", "sl": 1,
                          "zyId": "CMRM…_8hex_8hex_5hex",
                          "bzmc": "lxzymc", "llbznm": "mxlx",
                          "AmDp": [{ModelName, ModelType, count}],   // 按型号聚合
                          "Hang": [{ModelName, ModelType, count}],
                          "ammo": [{lxzymc, lxzynm, mxlx, mxmc, mxnm, sl: count}] } ] }
      ] } },
    { "dwsxnm": "02", "...红方同上" } ]
}
```

规则要点：

- 阵营映射 01→Blue、02→Red（可配置约定）；无实体的一方输出空 `trooplist` 数组。
- 每地图实体 = 一个 trooplist + 一个 `sl=1` 主装备；挂载实体按 `mountKind` 归入 `AmDp/Hang/ammo` 三个分支，**同型号聚合为 count**（Hang/AmDp 用 ModelName/ModelType 样例格式，ammo 用全字段+sl）。
- 不生成样例中不存在的环境/气象/链路字段；内部说明、校验状态不混入。

### 6.3 指令 JSON 结构

```jsonc
{ "rules": [ {
  "fanm": "uuid",
  "ybnm": "…想定同名…",
  "ruledata": [                          // 按"执行实体"分组
    {
      "action": [                        // 该实体的每条行动
        {
          "action_id": "ACT_xxx", "rule_type_code": "000680",
          "related_force": ["hs", "kd"],
          "executor": [{ "executor_id": "llbznm或zyId",
            "formation_struct": [{ "formation_id": "FORM_…", "entity_count": 1,
              "entity_name": "mxmc", "entity_type": "commandType|mxlx" }],
            "mount": [{ "mount_type": "mxlx", "mount_type_name": "mxmc", "num": n }] }],
          "target": [{ "target_id": "对方引用ID" }],
          "launch": [ { "target_id": "…", "target_child_id": "",
                        "weapon_type": "mxlx", "weapon_num": n, "way_points": [] } ],  // 启用武器时
          "route": [ { "points": [{ longitude, latitude, height, order }] } ],           // 有航路时
          "start_time": 0, "end_time": 600                    // 相对秒，恒输出
          // 配置型行动：另有 region / radar_angle / jam / supply / up/downstream 启用分支
        } ],
      "ruleconfig": [ { "is_auto_attack": true, "is_auto_return": {...},
        "is_task_continue": true, "is_hidden_maneuver": false, "is_consider_environment": false,
        "is_auto_deterrence": false, "is_deterrence_auto_return": false,
        "radar_detect_mode": { "detect_mode": "scan", "target_list": ["对方引用ID…"] },
        "intercept_strategy": 1.0 } ]   // 按"执行者+相同策略"分组；无启用字段时 []
    }
  ] } ]
}
```

规则要点：

- 执行者/目标引用统一走 `entityId`/`referenceId`（有 bzlx 用 llbznm，否则 zyId）；`formation_struct` 与 `mount` 由实体派生，允许用户编辑。
- 配置型行动（带 `definition`）经 `configuredOutput` 展开：`action_id` 取实例 ID，参数与策略字段按 `projectValue` 整枝省略未启用分支；**type 中 false 的字段既不出表单也不进输出**。
- 示例行动（无 definition）输出上述最小结构：`related_force` 写死 `hs/kd`，`ruleconfig` 用首版默认策略（`intercept_strategy 1.0`、雷达扫描模式等）。
- 输出严格 JSON（UTF-8，无注释/尾逗号/NaN），数值保持数值类型。

---

## 7 校验设计

三层校验，全部为纯函数、前后端可复算：

### 7.1 行动类型（`validateActionType`）

6 位数字代码、非空名称、flag 全布尔、无未知字段。后端入库前与建表 CHECK 双保险。

### 7.2 行动实例（`validateConfiguredAction`）

| 类别 | 规则 |
| --- | --- |
| 一致性 | 快照代码 === 行动 code；`executor_id` 必须等于已选实体的引用 ID |
| 结构 | 启用字段逐一 `validateValue` 递归（数量区间、数值范围、必填） |
| 引用 | 目标/打击目标必须是当前方案的对方阵营地图实体，且打击目标 ⊆ 行动目标 |
| 库存 | `executor.mount` 型号必须在实体实际武器挂载内且 num ≤ 实际数；同型号不得重复声明；`launch` 使用量 ≤ 对应 mount 声明量 |
| 时间 | `start_time ≤ end_time`，且 ≤ 方案总时长（秒） |
| 几何 | 区域 ≥3 个不同边界点；区域/航路/武器途经点 order 不重复；雷达角度起止有序 |
| 特化 | 返航机场须为本阵营机场实体；补给型号与数量一一对应 |

### 7.3 方案整体（`validate`）

- 基本信息：名称 1–60 字符；结束晚于开始。
- 实体树：引用 ID 全局唯一；挂载父必须存在且为地图实体、阵营一致、关系×模型合法、AmDp 要求父已有 Hang；坐标范围合法；挂载实体不得兼任编队。
- 行动：执行者必须是地图实体；目标非空、非己方、非挂载；经典行动时间 0 ≤ start ≤ end ≤ 总时长；武器为已挂载实体且 `count` 正整数。
- 库存汇总：按执行实体聚合所有行动的武器消耗（`actionConsumption`），超挂载数 → 实体级错误。
- 警告（不阻断）：同实体行动时间重叠；未配置真实判读规则。

生成（`generate`）以 errors 为空为前提；运行入队前再次 `validate` 全部所选方案。

---

## 8 运行与外部集成设计

### 8.1 当前实现：前端模拟

- 入队 `enqueue(plans, outcome)`：逐方案 `validate` 硬校验 → 逐方案生成文件 → 建 `run_{uuid}` 记录（含完整 plan 快照 + files 快照 + batchId），日志注明"前端模拟"。
- 串行执行（并发 1）：`queued → running → collecting → done | failed`；`complete()` 按 `outcome`（success/missing/error）为每行动合成事件证据（`mock:true` 标注）与判读（pass/fail/review）。
- 总体结论：有 fail → fail；全 pass → pass；否则 review。
- 重载语义：启动时所有 `queued/running/collecting` 改写为 `interrupted`（日志"模拟执行已中断"）——**重载永不把未完成变成成功**。

### 8.2 目标态：外部仿真器（接口约定）

| 约定 | 内容 |
| --- | --- |
| 调用形式 | 可执行程序 + 参数列表；参数模板含 `想定路径/指令路径/输出目录/运行编号/ybnm` 占位符，实际开关由设置填写，不预设 |
| 完成判据 | 启动与退出协议成功 ∧ 约定输出已产生 ∧ 输出属于本次运行 ∧ 文件可完整解析 ∧ 完成条件满足；`simulatorStatus=1` 不作固定完成标识 |
| 隔离 | 支持独立输出目录直接隔离；只能写固定目录时由串行适配器识别并归档本次产物后放行下一项 |
| 异常 | 启动失败、非零退出、超时均展示可读原因 + 原始日志；不自动重试，重跑由用户发起 |
| 运行目录 | `runs_root/{run_id}/{input/scenario, input/command, raw/, normalized/, manifest.json, run.log, verdict.json, summary.html}`；提交后冻结输入，外部程序只消费本次文件 |

状态机（目标态全集）：

```text
queued → starting → running → collecting → judging → done
   │        │          │            │           │
   └────────┴──────────┴────────────┴───────────┴→ cancelling → cancelled
                 异常分支：failed / timeout / artifact-error / judge-error / interrupted(重启)
done 仅表示流程结束；行动通过与否在逐行动判读层
```

### 8.3 判读设计

- 关联方式：行动 `action_id` ↔ 事件 `srcID`（执行实体引用 ID）、目标 `targetID`、时间窗内事件类型集合（初始事件目录 `CMRE000000001` 报告事件、`CMRE000000003` 实体毁伤事件）。
- 判读规则配置（`judgements`）：行动代码、事件代码集合、对象字段角色、时间窗口、成功/失败条件、关联方式；条件支持存在性/数值比较/字符串等值/行动终态与 AND/OR 组合，**不执行任意脚本**。
- 结论矩阵（六态，避免误判的核心）：

| 情况 | 结论 |
| --- | --- |
| 唯一匹配且满足成功规则 | 通过 |
| 唯一匹配且满足失败规则 | 不通过 |
| 找到事件但无终态/无规则 | 待复核 |
| 输出完整、规则要求必有事件而缺失 | 不通过 |
| 无规则 / ID 不明 / 多重匹配 | 待复核 |
| 运行或产物错误 | 未判读或判读异常 |

成功与失败条件同时成立且无终态优先级 → 待复核。总体结论按行动汇总（有确定失败→不通过；否则有待复核→待复核；全通过→通过；存在未判读且无确定失败→判读不完整），未判读项单独显示不被汇总掩盖。

### 8.4 复盘

用户点击显式调用外部复盘程序（独立适配器），与自动判读平行；当前演示为浏览器内模拟轨迹。复盘产物进入运行详情"产物"页。

---

## 9 HTTP 接口

| 方法 | 路径 | 请求 | 响应 |
| --- | --- | --- | --- |
| GET | `/api/health` | — | `{status, database:'sqlite', modelCount, planCount}` |
| GET | `/api/models` | `q`、`page`、`pageSize`、`FirstCfn..ForthCfn` | 传分页参 → `{items,total,page,pages,pageSize}`；否则 `{items:[…]}` |
| POST | `/api/models` | 型号对象（6 必填 + 可选） | 201 `{item}`（补 id=mxlx） |
| POST | `/api/models/import` | 型号数组 | 201 `{items}`；批内重复或校验错整批 400 |
| GET | `/api/action-types` | `q`、`page`、`pageSize` | 同上分页语义 |
| POST | `/api/action-types` | 完整类型（代码+名称+全部 flag 布尔） | 201 `{item}`；代码重复 409 |
| PUT | `/api/action-types/:code` | 完整类型 | 200 `{item}`；代码不可变更；未登记 404 |
| GET | `/api/plans` | — | `{items}` 按 archived, updated_at 排序 |
| POST | `/api/plans/import` | 方案数组 | 201 `{items}`；批内重复 ID 400；只补缺失 |
| GET | `/api/plans/:id` | — | 200 `{item}` / 404 |
| PUT | `/api/plans/:id` | 完整方案 | 200 `{item}`；路径与体 ID 不一致 400 |
| POST | `/api/generate` | 完整方案 | 200 `{files:{ybnm,scenario,commands,revision,created}}`；未登记代码/校验失败 400 |

错误响应统一 `{error: message}`；413（>25MB）、400（空体/坏 JSON）、404（未知 `/api/*`）。全部响应 `Cache-Control: no-store`。

---

## 10 部署与打包设计

`scripts/build-offline-linux.sh [amd64|arm64]`：

1. 从 nodejs.org 下载 node v22.23.2 二进制 tarball 至 `.cache/offline-linux/`（缓存复用），按官方 SHASUMS256 验证哈希，失败即终止。
2. 组装 `simtest-offline-<version>-ubuntu22.04-<arch>/`：`app/`（frontend+backend+scripts+data+docs）、`runtime/bin/node`（内嵌）、`seed/`、`bin/simtestctl` 启动控制脚本。
3. 产出 `dist/simtest_<ver>_<arch>.deb`（含 `debian/{control.in,postinst,prerm,postrm,conffiles}` + `simtest.service` systemd unit + `simtest.default` 环境文件）与离线 tar.gz，附 `.sha256` 校验文件。
4. 目标机完全离线可用：deb 安装或 `run-portable.sh` 便携运行，`verify.sh` 自检（health 接口 + 数据库）。

运行形态：本机开发 `npm start`（127.0.0.1:4173）；Linux 服务 `simtestctl start`（systemd 托管）。单用户、单进程、单 SQLite 文件（WAL + busy_timeout 5s）——这是明确的首版容量上限，不做多副本。

---

## 11 测试设计

| 层 | 手段 | 现状 |
| --- | --- | --- |
| 领域逻辑 | `tests/domain.test.mjs`（129 行）：ID 规则、挂载规则、校验矩阵、生成结构、copyPlan 引用重写、ZIP | 已覆盖 |
| 行动契约 | `tests/action-config.test.mjs`（114 行）：类型校验、字段开关省略、策略分组、编号唯一 | 已覆盖 |
| 存储层 | `tests/model-store.test.mjs`、`plan-store.test.mjs`：归一化缺省、原子批量、upsert、importMissing 幂等（临时文件库） | 已覆盖 |
| HTTP 契约 | `tests/mock-api.test.mjs`（75 行）：`createSimTestServer` 工厂 + 临时库起真实服务器，遍历全部路由与错误码 | 已覆盖 |
| 浏览器端到端 | `output/playwright/*.js` + 截图：主流程、挂载关系、边界交互、行动配置、独立测试库 | 已存档 |
| 部署 | `packaging/linux/verify.sh`：安装后 health + 数据库自检 | 已覆盖 |

合计 **35 个 node --test 用例，当前全部通过**。`/api/generate` 复用前端 `generate` 的设计使领域测试同时覆盖前后端两条路径。

---

## 12 已知边界与演进路线

当前实现与产品设计目标之间的差距（按后端接入点标注）：

| 演进项 | 现状 | 目标 |
| --- | --- | --- |
| 方案并发控制 | 全字段 upsert | 乐观锁（revision 冲突检测）+ 历史版本 |
| 文件发布 | 响应返回 JSON / 前端 ZIP 下载 | 后端原子写入 `scenario_root/command_root`，运行目录冻结 |
| 运行持久化 | localStorage 模拟 | 运行队列服务：进程/服务适配器、超时取消、推送/轮询进度 |
| 仿真器/复盘调用 | 模拟 | §8.2/8.4 适配器（占位符模板、产物归档、完成判据） |
| 判读规则 | 模拟结论 | `judgements` 配置表 + 规则引擎（§8.3 六态矩阵） |
| 设置 | localStorage | 设置服务：路径、参数、引用校验 |
| 型号治理 | 新增/导入/查询 | 编辑、停用、版本管理 |
| 容量 | 单进程单库、地图 ≤ 数千标记（聚类） | 多库/多副本不在首版范围 |

明确的"不做"（首版边界）：原型中的审定/出版流程不复制；不执行判读脚本；不自动重试失败运行；不读取可编辑草稿供外部程序；无规则的行动输出"待复核"而非臆断结果。