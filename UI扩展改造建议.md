# 大量装备、行动与实体的界面改造建议

评估日期：2026-09-10。实施日期：2026-09-10。本文提出的高频查找、分页、折叠、批量操作和地图聚合已完成第一版前端实现；需要方案后端化或专门渲染架构的项目保留为后续阶段。

## 本次实施结果

| 区域 | 已实现行为 |
| --- | --- |
| 装备目录 | 默认紧凑表格，支持名称搜索、四级分类筛选、表格/卡片切换和每页 50 条分页 |
| 添加实体 | 四级分类均可选“全部”，支持直接搜索装备；挂载模式可搜索所属主实体，装备与主实体结果最多展示前 100 条并提示总数 |
| 行动目录 | 支持行动名称、代码和启用字段搜索，每页 50 条，表格只展示字段数量与摘要 |
| 添加行动 | 行动类型可搜索；执行实体支持名称/装备搜索、阵营筛选、每页 50 条、跨页保留选择和选择全部筛选结果；已选实体独立展示 |
| 实体管理 | 地图/表格切换；名称、阵营、型号和状态筛选；按阵营、型号或分类分组；挂载默认折叠并按关系与型号汇总；每页 50 个主实体 |
| 行动管理 | 名称、代码、执行实体、阵营、类型、校验状态和时间窗口筛选；时间/名称排序；按执行实体分组折叠；每页 50 条；支持批量复制、删除和时间平移 |
| 参数表单 | 按行动时间、行动参数、行为策略分区折叠；开始和结束时间固定必填，单批最多创建 1000 条行动 |
| 地图 | 默认只显示选中或悬停实体名称；支持“显示全部名称”；重叠点位聚合，点击聚合点放大；方案内保留地图中心和缩放 |

同一套 1440 × 1000、500 个装备型号、200 种行动类型、200 个地图实体、400 个挂载实体、1000 条行动的临时数据已重新验收：装备、行动类型、实体和行动均只渲染当前 50 条；实体地图由 1 个独立标记和 12 个聚合点呈现，默认只有选中实体名称可见；行动执行实体选择器当前页为 50 条、可见区约 208 像素。浏览器控制台无错误。复验脚本及截图位于 `output/playwright/ui-scale-review.js`、`ui-scale-interactions.js`、`ui-scale-map.png` 和 `ui-scale-action.png`。

## 结论与评估范围

需要调整信息组织和操作方式。当前界面适合验证流程；目录、实体选择和行动表格均一次性铺开，数量增加后查找和操作成本会明显上升。建议沿用现有导航、配色和业务规则，优先加入搜索、筛选、折叠、分页及批量选择，再优化地图渲染和方案存储。

用户尚未指定最终规模，先按单场 100–500 个地图实体规划，挂载实体另计。本次在独立浏览器中构造 500 个装备型号、200 种行动类型、200 个地图实体、400 个挂载实体、1000 条行动。全部为临时模拟数据；目录接口被模拟，不写入实际业务数据库。

1440 × 1000 窗口下的布局检查结果：

| 区域 | 当前表现 | 使用影响 |
| --- | --- | --- |
| 装备目录 | 500 张卡片全部渲染，整页约 58,800 像素高，无搜索框 | 分类较多时仍需要长距离滚动 |
| 行动目录 | 200 行全部展开，整页约 14,100 像素高 | 找行动及比较字段配置困难 |
| 实体列表 | 600 个主实体及挂载条目全部展开；内容约 35,400 像素高，可见区域 580 像素 | 挂载条目占用大量空间，定位主实体困难 |
| 添加行动 | 200 个执行实体以复选卡片排列，选择区约 7,600 像素高；行动下拉含 200 个选项 | 找实体和进入参数区都需要反复滚动 |
| 行动列表 | 1000 行全部展开，整页约 70,600 像素高 | 无法快速查看某个实体、行动类型或时间段 |
| 地图 | 同时显示 200 个名称标签，密集部署区域明显重叠 | 难以辨识和点选 |

以上为布局观察，不是帧率、响应时间或系统容量上限测试。截图和复现脚本在 `output/playwright/ui-scale-map.png`、`ui-scale-action.png`、`ui-scale-review.js`。

## 建议的页面组织

| 位置 | 内容 |
| --- | --- |
| 全局“模型与行动” | 保留装备目录、行动目录，负责维护可选类型 |
| 方案内“想定编辑” | 增加“地图 / 实体表格”切换，共享筛选和选择状态 |
| 方案内“行动指令” | 以可筛选表格为主，支持按执行实体分组 |
| 复杂编辑区 | 使用宽侧栏或独立编辑区，分区填写；小型新建操作保留弹窗 |

想定编辑继续采用三栏结构：左侧查找和选择实体，中间地图或表格，右侧显示所选实体属性。三栏宽度可调，地图支持展开。筛选栏和批量工具栏保持可见。

## 第一批：优先解决查找和选择（主要能力已实施）

### 装备目录与添加实体

- 目录默认紧凑表格，提供四级分类筛选和名称搜索，当前固定每页 50 条。详情通过弹窗查看，卡片作为可选视图。
- 添加实体时，保留四级分类，但各级允许“全部”；用户也可直接搜索名称，不必先逐级选完。
- 装备选项主名称继续使用显示名称，分类用于区分。同名型号在详情中核对，保留稳定的内部选择标识。
- 选择装备后配置阵营、数量、是否编队，再布置到地图。挂载入口复用同一套可搜索选择器。
- 后续可增加“最近使用”，优先来源于当前用户操作记录。

### 行动目录与添加行动

- 行动目录支持名称、行动代码搜索，列表只显示名称、代码、启用字段数量及编辑入口；完整字段进入详情查看。
- 不把装备四级分类套用于行动。第一批使用搜索和常用记录，业务明确后再增加独立行动分组。
- 添加行动采用“选择类型与执行实体 → 配置参数 → 查看展开摘要”的流程。
- 执行实体先按阵营、装备型号及名称筛选，左侧显示搜索结果，右侧显示已选实体，保留跨页选择。
- “全选当前页”和“选择全部筛选结果”明确区分，持续显示选中数量。
- 同一批仍限定同阵营、同型号；选择首个实体后，其他阵营或型号显示不可选原因。
- 目标和挂载所属主实体也使用同一套搜索选择组件，避免在其他入口继续出现很长的下拉框。

## 第二批：管理一场仿真中的大量实例（核心功能已实施）

### 实体列表与表格

- 默认按阵营分组，可切换按分类或型号分组，支持名称搜索、只看未配置行动、只看校验错误。
- 主实体下挂载默认折叠，按关系和型号汇总显示，例如“机库：某型飞机 × 8”；展开后再查看各个实例。
- 界面汇总不合并实际数据，每个挂载实例继续保持自己的编号。管理分组也不改变实体是否属于编队。
- 实体表格展示名称、阵营、装备、分类、坐标、挂载数量、行动数量及校验状态，便于集中核对。
- 选择实体时地图定位并高亮；切换选中项保留地图视野和列表滚动位置。
- 增加批量设置共同属性、批量配置挂载、批量添加行动。批量编辑仅修改用户明确选择的字段，并预览受影响数量与引用变化。

### 行动列表与表单

- 表格支持执行实体、阵营、行动类型、时间区间、校验状态筛选，以及时间排序和分页。
- 按执行实体分组时，先显示行动数量和时间摘要，按需展开。
- 批量操作优先提供复制、删除、时间平移和修改共同参数；每条新行动仍独立分配编号，整批校验后保存。
- 参数编辑分为“基础信息与时间、目标与武器、区域与航路、行为策略”，只显示当前类型启用的分区。
- 开始、结束时间保持固定必填；编队构成保持可选。错误提示定位到具体字段和条目。
- 时间轴作为后续辅助视图，先保证表格能完成所有操作。

## 第三批：地图与性能（交互与持久化已实施）

- 默认只显示所选或悬停实体名称，提供“显示全部名称”开关；密集区域显示数量聚合，放大或展开后选择具体实体。
- 支持阵营、型号筛选、框选、全屏和可调栏宽。空间聚合仅影响显示，不生成编队。
- 实体树、结果列表和行动表格使用每页 50 条的分页；型号与行动目录的后端分页返回总数和当前页。
- 地图中心、缩放及选择状态独立保存；连续拖动和平移更新使用 `requestAnimationFrame` 合并。
- 当前地图仍使用 SVG 并在单帧中重绘底图和实体层；达到更高规模时是否分层或替换 Canvas/WebGL，应由后续基准测试决定。
- 方案、实体和行动实例已作为完整快照写入 SQLite，支持按 ID 读取和更新；浏览器旧数据按 ID 幂等补迁移。

## 验收场景与实施顺序

建议先完成目录搜索、分页以及通用选择器，再做实体分组与挂载折叠、行动筛选及分区表单，最后补地图聚合、虚拟滚动和后端按需读取。

以本次模拟数据复验，并另外加入同名型号、密集同坐标、无目标行动、部分实体库存不足等场景：

1. 可通过搜索直接定位装备、行动类型和实体，无需滚到列表底部。
2. 分页或筛选后已选项可见且可撤销，批量操作范围明确。
3. 多选同型号实体批量建行动时，编号、时间、挂载库存和引用规则保持正确。
4. 折叠和汇总挂载不改变实例数量及生成 JSON。
5. 地图名称不遮挡所有点位，聚合区域能展开选择；修改属性不重置视野。
6. 保存、重新打开、复制、生成文件后数据一致；界面性能在指定电脑和规模下单独记录。

相关实现入口：`frontend/app.js` 的 `modelCatalog`、`mapTab`、`entityDialog`、`actionsTab`、`render`；`frontend/action-ui.js` 的行动目录及选择表单；`frontend/map.js` 的 `draw`；`frontend/api.js` 的数据读取与浏览器持久化。

## 保留边界

- 实体树采用分页和挂载按需展开，尚未使用虚拟滚动；当前目标规模下已把一次渲染量限制在 50 个主实体。
- 地图连续交互已合并到每个动画帧，但底图和实体 SVG 仍在该帧重绘；局部图层、空间索引及 Canvas/WebGL 需结合更高规模基准决定。
- 领域校验仍会按实体、父子挂载和行动做多次全量查找；单方案上升到数千实体时，应补充内存索引并单独基准测试。
- 型号和行动目录页已使用后端搜索与分页结果；新建实体和行动弹窗为了跨分类选择，启动时仍会缓存完整目录。如目录达数万条，需将弹窗选择也改为按需查询并单独提供分类聚合接口。
