# 仿真自动化测试需求描述

流程

@/Users/hjt/Projects/自动化测试/自动化测试工具_界面原型_v0.6.html

按照该文件流程进行

仿真器为外接程序，

启动该仿真器需要接入想定，和指令，示例如 @/Users/hjt/Projects/自动化测试/模版文档 下的文档所示



## 1、想定生成

首先需要构造想定，想定中的字段按照示例构造，具体含义可参考

@/Users/hjt/Projects/自动化测试/模版文档/仿真通用模版总结.docx，只构造示例中的字段其它不用管，构造时模型直接加载到前端，留好扩展，后续可加入其它模型，前端显示mxmc,并映射到mxlx上，增加其它模型时直接加载到前端，并自动生成一个当前想定中唯一一个 llbznm ，如果 bzlx 为空，则是zyid ，用户通过地图选点的方式创建该实体及一个trooplist，挂载武器或飞机也按照格式说明，在前端可操作。后生成。且生成一个方案标题写到 ybnm

## 2、指令生成

行动指令按照示例所示，其中创建时为选择为上述创建的一个或多个实体创建一个行动或多个行动及一个ruledata，其中选择的实体生成指令时他的 llbznm

或zyid 自动填入executor_id，并为该实体创建一个指令，其它如 rule_type_code 指代的是行动代码，先构建一个映射表，留好接口， 后续填入行动名称和对应关系，方便用户建立选择行动，该行动开始时间最小为0，最大为上述想定中的结束时间于开始时间差值的秒数，action_id生成该指令文件中唯一字符串，其余字段按照行动的不同让用户选填或选择，target_id 让用户选择必须是已创建对方阵营的 llbznm 或zyid，武器必须是想定中已挂载的武器，且数量不能超过想定中的挂载数量，参考示例和说明，不变的字段可直接写死。

## 3、调用仿真器

生成文件后调用仿真器进行推演，需要留调用接口，生成产物的两个文件名字均为 {ybnm}.json，上层路径写入配置文件，由于可批量调用需 ybnm不能相同。

## 4、复盘推演

拿到产物后调用另一个复盘推演程序复盘。

## 5、结果判读

同时对产物进行初步判读，重点检查是否有该行动事件，及行动事件是否成功。如果有错误，进行标识。

