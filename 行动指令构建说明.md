# 行动指令构建

本阶段依据《需求描述之指令生成.md》、仿真通用模版总结及当前指令 JSON 示例实现。行动类型保存到本地 SQLite，行动实例随所属方案整体入库；生成配套 JSON 已接入后端。

## 操作流程

1. 打开“模型与行动 → 行动目录 → 添加行动类型”，填写六位数字行动代码和名称。
2. 勾选该行动需要的行动字段、策略字段。保存后写入数据库。
3. 打开方案的“行动指令 → 添加行动”，选择行动类型及执行实体，填写显示的参数。
4. 批量添加时，多选同一阵营、同一具体型号的地图实体，设置每个实体的行动条数（1–50）。例如 2 个实体 × 3 条行动，保存为 6 条独立行动。
5. 保存后可逐条编辑、复制或删除。点击“生成文件”，通过后端校验后预览并下载配套 JSON / ZIP。

初始提供 `000680 / 示例行动`。武器使用、区域、航路、雷达角度、上下游分类、补给、干扰默认关闭，可以在行动目录中启用，也可以新增独立类型。

## 行动类型表

数据库：`data/simtest.sqlite`，新增业务表：`action_types`。原有 `equipment_models` 保留。

一条记录描述一种行动；`rule_type_code` 是唯一主键，`xdmc` 为显示名称。其余 21 列均为布尔标记，SQLite 使用受 CHECK 约束的整数 0/1 存储，HTTP 接口返回 JSON boolean。

| 来源 | 字段 |
| --- | --- |
| 标识 | `rule_type_code`、`xdmc`（字符串） |
| action | `action_id`、`related_force`、`executor`、`target`、`launch`、`region`、`route`、`radar_angle`、`upstream_entity`、`downstream_entity`、`supply`、`jam` |
| ruleconfig | `is_auto_attack`、`is_auto_return`、`is_task_continue`、`is_hidden_maneuver`、`is_consider_environment`、`is_auto_deterrence`、`is_deterrence_auto_return`、`radar_detect_mode`、`intercept_strategy` |

`start_time` 和 `end_time` 不属于行动类型表，所有行动固定必填，并始终写入生成的 action 对象。现有数据库启动时自动移除这两列，保留其它配置和记录；已有行动保留已填写的时间。

`true` 表示该类型必须填写该字段，前端显示其全部嵌套项目；`false` 表示不显示，生成时省略该字段。标记和实例值分开：类型中的 `is_auto_attack=true` 表示显示“自动攻击”，该条行动仍可以选择“否”，最终输出 `is_auto_attack:false`。

行动编号由系统为每个实例生成，启用 `action_id` 时输出，不启用时省略；内部编号始终存在且唯一。执行者引用随所选实体自动绑定，防止指令指向其他实例。名称 `xdmc` 供界面使用，不加入示例中没有此字段的 action 对象。

行动目录支持新增及修改字段配置。每条行动保存创建时的类型快照，修改目录只影响之后新增的行动；已有行动继续按自己的快照编辑、校验和生成。

## 参数与校验

所有嵌套对象通过表单编辑，数组可添加和删除条目。字段结构与示例一致；未知字段不进入生成结果。

- 执行者：一条行动一个地图实体。编队构成为可选项，可删除全部条目；未配置时省略 `executor.formation_struct`，填写时校验成员字段。可编辑挂载声明；声明武器必须实际存在于该实体直接挂载或武器库中，数量不能超过库存。
- 目标：从对方阵营的地图实体选择；武器使用记录可逐条配置目标、子单元字符串、武器和数量。启用行动目标列表时，打击目标必须属于该列表。
- 时间：相对仿真开始的秒数；始终必填并检查时长范围，开始不晚于结束。
- 区域与航路：经纬度、高度、顺序和区域类型均可填写；区域至少三个不同坐标，航路至少一个点，同一组点的顺序不重复。
- 雷达与干扰：四个角度、探测模式及目标、干扰类型和模式均可修改。
- 补给：比例、武器型号列表和数量列表；型号与数量一一对应。预计补给不用于放宽初始库存限制。
- 策略：实际布尔值由用户选择；开启自动返航必须指定本阵营的机场实体。
- 库存：合计同一实体所有行动的显式武器使用量，批量创建也按整批检查。

空的目标子单元、关闭返航时空机场、无挂载清单、无武器途经点保留样例允许的空值形状。模型分类与标识继续从当前装备和实体目录读取。

## 生成规则

后端从一次请求中的同一份方案构建想定和指令，保持文件标题和实体引用一致。新格式行动按“执行实体 + 完全相同的策略值”分组为 `ruledata`；不同策略不会合并覆盖。每组 `action` 可含多条行动，`ruleconfig` 为一个策略对象；未启用任何策略时为 `[]`。

同一实例反复生成保持行动编号稳定；复制行动或方案生成新编号，并重写方案副本中的实体引用。旧示例行动仍兼容生成，编辑保存后转换为新表单格式。

后端保存型号目录、行动目录和完整方案；方案 JSON 包含行动的类型快照和实例值。生成接口返回文件内容供预览下载，尚不向配置目录发布文件。模拟运行和复盘仍使用演示数据。

## 接口

| 方法 | 路径 | 请求 / 返回 |
| --- | --- | --- |
| GET | `/api/action-types` | 返回 `{items: [...]}`；传 `q`/`page`/`pageSize` 时返回分页结果 |
| POST | `/api/action-types` | 提交完整类型对象，返回 `{item: ...}`；重复代码返回 409 |
| PUT | `/api/action-types/:code` | 提交完整类型对象，代码不能更改；返回 `{item: ...}` |
| POST | `/api/generate` | 提交完整方案（含实体、行动及配置快照），返回 `{files: ...}` |

类型写入需提交全部布尔字段，字符串 `"true"` 不等同于布尔值。生成时校验代码已登记、嵌套字段、当前实体引用、时间、库存和坐标；非法配置返回 400，不返回半成品。

```bash
npm start
curl http://127.0.0.1:4173/api/action-types
npm test
```

35 项自动化用例覆盖领域逻辑、数据库和 HTTP 接口，包括行动类型、全部示例字段、字段省略、策略分组、编号唯一性、复制、方案入库与刷新恢复及错误校验。浏览器验证脚本位于 `output/playwright/`，使用独立测试数据库和浏览器存储。
